<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head') %>
  <body>
    <%- include('../partials/nav') %>
    <!-- Showcase -->
    <section
      class="bg-dark text-light p-5 p-lg-2 pt-lg-5 pt-sm-5 text-center text-sm-start bbg"
    >
      <div class="container">
        <div class="d-sm-flex align-items-center justify-content-between g-4">
          <div class="px-2">
            <h1>
              Make a<span class="text-warning">
                good choice for a best Future
              </span>
            </h1>
            <p class="lead my-4">
              What is your dream for the future? who do you want to become in
              the comming Years? The Answere for thise will determine who you
              will become tomorrow.
            </p>
          </div>
          <img
            class="img-fluid w-50 d-none d-md-block px-2"
            src="https://www.theasianschool.net/blog/wp-content/uploads/2023/01/Books-Help-To-Get-More-Focus.jpg"
            alt="community picture"
          />
        </div>
      </div>
    </section>
    <section class="pt-2">
      <div class="container pt-5">
        <!-- Your content goes here -->
         <h1 class="border-bottom"><%=title%></h1>
        <div class="courses content">
          <% if (courses.length > 0) { %> <% courses.forEach(course => { %>
          <a class="single" href="/courses/<%= course._id %>">
            <h3 class="title"><%= course.name %></h3>
            <p class="snippet"><%=course.subject %></p>
          </a>

          <% }) %> <% }else{ %>
          <p>No courses has been publish from any teachear</p>
          <% } %>
        </div>
      </div>
    </section>

    <%- include('../partials/footer') %> <%- include('../partials/script') %>
  </body>
</html>
